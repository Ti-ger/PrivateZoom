/*SEAMLESS_ZOOM — A technique for seamless zooming between process models and process instances.Copyright (C) 2025  Christoffer RubenssonThis program is free software: you can redistribute it and/or modifyit under the terms of the GNU Affero General Public License aspublished by the Free Software Foundation, either version 3 of theLicense, or any later version.This program is distributed in the hope that it will be useful,but WITHOUT ANY WARRANTY; without even the implied warranty ofMERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See theGNU Affero General Public License for more details.You should have received a copy of the GNU Affero General Public Licensealong with this program.  If not, see <https://www.gnu.org/licenses/>.Website: https://hu-berlin.de/rubenssonE-Mail: {firstname.lastname}@hu-berlin.de*/// -----------------------------// MAIN DRAWING FUNCTION// -----------------------------// == IMPORT ==import {exportData} from './utils/exportData.mjs';import {ABSTRACTEDMAP} from "./views/AbstractedMap.js";import {getAccessors} from "./utils/parserGenerator.mjs";let accessors;const ABSTRACTIONS = {    abstractions: []};const SPECIFIC_ZOOMS = {  specific_zooms: []};// == MAIN GRAPH DRAWING FUNCTION ==async function draw() {    // == INITIALIZATION ==    d3.select("#chart").selectAll("*").remove();    const payload = {        abstractions: ABSTRACTIONS.abstractions,   // Array der gewählten Abstractions        specific_zooms: SPECIFIC_ZOOMS.specific_zooms // Array der definierten Specific Zooms    };    const response = await fetch('/api/abstracted_data', {      method: 'POST',      headers: {        'Content-Type': 'application/json'      },      body: JSON.stringify(payload)    });    console.log("Response received from server:", response.status);    let csvdata = await response.json();    console.log("Server response:", csvdata);     if (!response.ok) {      switch (csvdata.error.code) {        case "PRIVACY_REQUIREMENTS_NOT_SATISFIED":          alert("Privacy Budget exceeded")          break;        case "CYCLE_DETECTED_IN_ABSTRACTION_DEPENDENCIES":          alert("Cycle detected in abstraction dependencies. Please adjust your abstraction selections.")          break      }    }    console.info("Switching to Abstracted Map");    d3.select("#chart").selectAll("*").remove();    let x_selector = document.getElementById("x-axis-select");    let selected_x_accessor = x_selector[x_selector.selectedIndex].value;    let y_selector = document.getElementById("y-axis-select");    let selected_y_accessor = y_selector[y_selector.selectedIndex].value;    let x_accessor = accessors[selected_x_accessor];    let y_accessor = accessors[selected_y_accessor];    console.info("Selected x-axis accessor:", selected_x_accessor);    console.info("Selected y-axis accessor:", selected_y_accessor);    ABSTRACTEDMAP(csvdata, x_accessor, y_accessor, selected_x_accessor, selected_y_accessor)}async function initializeApp() {  await loadAvailableAbstractions();  await buildAttributeDropdowns();  await buildSpecificZoomDropdown();  accessors = await getAccessors();    const accessorKeys = Object.keys(accessors);    populateSelect("y-axis-select", accessorKeys);    populateSelect("x-axis-select", accessorKeys);  await draw();}document.addEventListener("DOMContentLoaded", initializeApp);// == EXPORT GRAPH ==// Export buttonconst exportButton = document.getElementById('button-export');exportButton.addEventListener('click', () => {  console.log("Exporting data...");  exportData();  console.log("Data exported successfully.");});// Redraw the chart with uploaded datadocument.getElementById('upload-form').addEventListener('submit', async function (e) {  const setupBar = document.getElementById("setup-bar");  e.preventDefault();  console.log("Uploading data...");  const fileInput = document.getElementById('file-input');  if (!fileInput.files.length) return;  console.log("File selected:", fileInput.files[0].name);  const formData = new FormData();  formData.append('file', fileInput.files[0]);  const file_name_span = document.getElementById("file-name")  file_name_span.textContent= fileInput.files[0].name;  const finishButton = document.getElementById("collapse-setup");  const editButton = document.getElementById("edit-setup");  // collapse when config finished  finishButton.addEventListener("click", () => {    setupBar.classList.add("collapsed");  });  // reopen config  editButton.addEventListener("click", () => {    setupBar.classList.remove("collapsed");  });  console.log("Form data prepared for upload.");  const response = await fetch('/api/upload_data', {    method: 'POST',    body: formData  });  await initializeApp();});// Reset the chart with default data/*const resetButton = document.getElementById('button-reset');resetButton.addEventListener('click', () => {  draw();});*/async function loadAvailableAbstractions() {  const response = await fetch("/api/available_abstractions");  const abstractions = await response.json();  const container = document.getElementById("abstractions-container");  container.innerHTML = "";  ABSTRACTIONS.abstractions = [];  Object.entries(abstractions).forEach(([type, options]) => {    const wrapper = document.createElement("div");    wrapper.classList.add("slider-row");    // Label    const label = document.createElement("label");    label.textContent = type + ":";    // Slider    const slider = document.createElement("input");    slider.type = "range";    slider.min = 0;    slider.max = options.length - 1;    slider.step = 1;    slider.value = 0;    const initialSelection = options[0];    if (initialSelection) {      ABSTRACTIONS.abstractions.push(initialSelection);    }    // Wert-Label    const valueLabel = document.createElement("span");    valueLabel.textContent = initialSelection      ? prettifyAbstractionName(initialSelection)      : "-";    valueLabel.style.fontWeight = "bold";    const ticksContainer = document.createElement("div");      ticksContainer.classList.add("slider-ticks");      options.forEach(opt => {        const tick = document.createElement("span");        tick.textContent = prettifyAbstractionName(opt);        ticksContainer.appendChild(tick);      });    // Eventlistener    slider.addEventListener("input", () => {      const selectedIndex = parseInt(slider.value, 10);      const selectedKey = options[selectedIndex];      valueLabel.textContent = prettifyAbstractionName(selectedKey);      // remove old abstraction of this type        console.log("vorher");        console.log(ABSTRACTIONS.abstractions);        console.log("type to remove:", type.split("_")[0]);        console.log("type", type);      ABSTRACTIONS.abstractions = ABSTRACTIONS.abstractions        .filter(a => !a.startsWith(type.split("_")[0]));        console.log("zwischen");        console.log(ABSTRACTIONS.abstractions);      // insert new      ABSTRACTIONS.abstractions.push(selectedKey);        console.log("nachher");        console.log(ABSTRACTIONS.abstractions);      console.log("new abstractions:", ABSTRACTIONS);      resetSpecificZooms();      buildSpecificZoomDropdown();      draw();    });    const label_div = document.createElement("div");    label_div.style.display = "flex";    label_div.style.justifyContent = "space-between";    label_div.style.alignItems = "center";    label_div.style.width = "100%";    label_div.appendChild(label);    label_div.appendChild(valueLabel);    // DOM zusammenfügen    //wrapper.appendChild(label);    wrapper.appendChild(label_div);    wrapper.appendChild(slider);    //wrapper.appendChild(valueLabel);    wrapper.appendChild(ticksContainer);    container.appendChild(wrapper);  });}function prettifyAbstractionName(key) {  const arr = key.split("_")  arr.shift();  const abs_name = arr.join(" ");  abs_name.replace(/\b\w/g, c => c.toUpperCase());  return String(abs_name).charAt(0).toUpperCase() + String(abs_name).slice(1);  return abs_name}function resetSpecificZooms() {  SPECIFIC_ZOOMS.specific_zooms = [];  const container_filter = document.getElementById("specific_zoom-filter-container");  container_filter.innerHTML = "";  const container_abstraction = document.getElementById("specific_zoom-abstraction-container");  container_abstraction.innerHTML = "";  renderSpecificZooms();}document.getElementById("y-axis-select").addEventListener('change', (event) => {    draw();});document.getElementById("x-axis-select").addEventListener('change', (event) => {    draw();});const populateSelect = (selectId, keys) => {  const select = document.getElementById(selectId);  console.log("KEYS:")  console.log(keys);  select.innerHTML = "";  keys.forEach(key => {    const option = document.createElement("option");    option.value = key;    option.textContent = key; // später schöner machen    select.appendChild(option);  });};async function load_attribute_types() {  const response = await fetch("/api/attribute_types");  const attribute_types =  await response.json()  return attribute_types}async function load_attributes() {  const response = await fetch("/api/attributes");  const attributes = await response.json()  return attributes}async function load_occurring_entries(col_name) {  const response = await fetch("/api/occurring_entries/" + col_name);  const occurring_entries = await response.json()  return occurring_entries}async function load_available_abstractions_for_column(col_name) {    const response = await fetch("/api/available_abstractions/" + col_name);    const available_abstractions = await response.json()    return available_abstractions}async function buildAttributeDropdowns() {  const attributeTypes = await load_attribute_types();  const attributes = await load_attributes();  const container = document.getElementById("attribute-container");  container.innerHTML = "";  for (const [attrName, currentType] of Object.entries(attributes.eventAttributes)) {    const wrapper = document.createElement("div");    const label = document.createElement("label");    label.textContent = attrName;    const select = document.createElement("select");    attributeTypes.forEach(type => {      const option = document.createElement("option");      option.value = type;      option.textContent = type;      if (type === currentType) {        option.selected = true;      }      select.appendChild(option);    });    select.addEventListener("change", async (e) => {      console.log(`Attribute ${attrName} changed to ${e.target.value}`)      // update in backend      const payload = {attribute : attrName, type: e.target.value};      const response = await fetch('/api/attribute_types', {        method: 'POST',        headers: {          'Content-Type': 'application/json'        },        body: JSON.stringify(payload)      });      // eigentlich jetzt alles neu laden -> Abstraktionsfunktionen neu berechnen, die ganzen Achsen, man müsste auch die Vorabberechnungen für zum beipsiel numerical Abstrkationen neu machen      await initializeApp();    });    wrapper.style.display = "flex";    wrapper.style.justifyContent = "space-between";    wrapper.style.alignItems = "center";    wrapper.style.width = "100%";    wrapper.appendChild(label);    wrapper.appendChild(select);    container.appendChild(wrapper);  }}async function buildSpecificZoomDropdown() {  const attributes = await load_attributes();  const container = document.getElementById("specific_zoom-container");  container.innerHTML = "";    const wrapper_target = document.createElement("div");    const label_target = document.createElement("label");    label_target.textContent = "Target Column";    const select_target = document.createElement("select");    select_target.id = "specific_zoom-target";    for (const [attrName, _] of Object.entries(attributes.eventAttributes)) {      const option = document.createElement("option");      option.value = attrName;      option.textContent = attrName;      select_target.appendChild(option);    }    select_target.addEventListener("change", async (e) => {      console.log(`Column ${e.target.value} selected for specific zoom Target Column.`)      const possible_abstractions = await load_available_abstractions_for_column(e.target.value);      console.log(`Possible abstractions for ${e.target.value} ` + possible_abstractions);      // build dropdown      await buildSpecificZoomAbstractionsSlider(possible_abstractions);    });    wrapper_target.style.display = "flex";    wrapper_target.style.justifyContent = "center";    wrapper_target.appendChild(label_target);    wrapper_target.appendChild(select_target);    container.appendChild(wrapper_target);    const wrapper_source = document.createElement("div");    const label_source = document.createElement("label");    label_source.textContent = "Source Column";    const select_source = document.createElement("select");    select_source.id = "specific_zoom-source";    for (const [attrName, currentType] of Object.entries(attributes.eventAttributes)) {      const option = document.createElement("option");      option.value = attrName;      option.textContent = attrName;      select_source.appendChild(option);    }    select_source.addEventListener("change", async (e) => {      console.log(`Column ${e.target.value} selected for specific zoom Source Column.`);      const occurring_entries = await load_occurring_entries(e.target.value);      console.log(`possible entries for ${e.target.value} ` + occurring_entries);      // build_dropdown      await buildSpecificZoomFilterDropdown(occurring_entries);    });    wrapper_source.appendChild(label_source);    wrapper_source.appendChild(select_source);    wrapper_source.style.display = "flex";    wrapper_source.style.justifyContent = "center"    container.appendChild(wrapper_source);}async function buildSpecificZoomFilterDropdown(occurring_entries) {  const container = document.getElementById("specific_zoom-filter-container");  container.innerHTML = "";  const wrapper = document.createElement("div");  const label = document.createElement("label");  label.textContent = "Filter Value";  const select = document.createElement("select");  select.id = "specific_zoom-filter";  for (const entry of occurring_entries) {    const option = document.createElement("option");    option.value = entry;    option.textContent = entry;    select.appendChild(option);  }  select.addEventListener("change", async (e) => {    console.log(`Entry ${e.target.value} selected for Filter.`)  });  wrapper.style.display = "flex";  wrapper.style.justifyContent = "center";  wrapper.appendChild(label);  wrapper.appendChild(select);  container.appendChild(wrapper);}async function buildSpecificZoomAbstractionsSlider(possible_abstractions) {  const container = document.getElementById("specific_zoom-abstraction-container");  container.innerHTML = "";  const wrapper = document.createElement("div");  wrapper.classList.add("slider-row");  // Slider  const slider = document.createElement("input");  slider.type = "range";  slider.id = "specific_zoom-abstraction";  slider.min = 0;  slider.max = possible_abstractions.length - 1;  slider.step = 1;  slider.value = 0;  const initialSelection = possible_abstractions[0];  slider.dataset.selectedAbstraction = initialSelection || "";  // Wert-Label  const valueLabel = document.createElement("span");  valueLabel.textContent = initialSelection      ? prettifyAbstractionName(initialSelection)      : "-";  valueLabel.style.fontWeight = "bold";  const ticksContainer = document.createElement("div");  ticksContainer.classList.add("slider-ticks");  possible_abstractions.forEach(opt => {    const tick = document.createElement("span");    tick.textContent = prettifyAbstractionName(opt);    ticksContainer.appendChild(tick);  });  slider.addEventListener("input", () => {    const selectedIndex = parseInt(slider.value, 10);    const selectedAbstraction = possible_abstractions[selectedIndex] || "";    slider.dataset.selectedAbstraction = selectedAbstraction;    valueLabel.textContent = selectedAbstraction      ? prettifyAbstractionName(selectedAbstraction)      : "-";  });  // DOM zusammenfügen  wrapper.appendChild(slider);  wrapper.appendChild(valueLabel);  wrapper.appendChild(ticksContainer);  container.appendChild(wrapper);}/*async function buildSpecificZoomAbstractionsDropdown(possible_abstractions) {  const container = document.getElementById("specific_zoom-abstraction-container");  container.innerHTML = "";  const wrapper = document.createElement("div");  const label = document.createElement("label");  label.textContent = "Abstraction Function";  const select = document.createElement("select");  select.id = "specific_zoom-abstraction";  for (const abstraction_function of possible_abstractions) {    const option = document.createElement("option");    option.value = abstraction_function;    option.textContent = abstraction_function;    select.appendChild(option);  }  select.addEventListener("change", async (e) => {    console.log(`Abstraction Function ${e.target.value} .`)  });  wrapper.appendChild(label);  wrapper.appendChild(select);  container.appendChild(wrapper);}*/document.getElementById("specific-zoom-submit").addEventListener('click', async (event) => {  event.preventDefault();  const target = document.getElementById("specific_zoom-target")?.value;  const source = document.getElementById("specific_zoom-source")?.value;  const filter = document.getElementById("specific_zoom-filter")?.value;  const abstractionSlider = document.getElementById("specific_zoom-abstraction");  const abstraction = abstractionSlider?.dataset?.selectedAbstraction || null;  console.log("Target:", target);  console.log("Source:", source);  console.log("Filter:", filter);  console.log("Abstraction:", abstraction);  const new_specific_zoom = {    "target_column": target,    "filter_column": source,    "filter_attribute": filter,    "abstraction_function": abstraction  };  SPECIFIC_ZOOMS.specific_zooms.push(new_specific_zoom);  renderSpecificZooms();  await draw();});function renderSpecificZooms() {  const table = document.getElementById("testBody");  table.innerHTML = "";  SPECIFIC_ZOOMS.specific_zooms.forEach((zoom, index) => {    let row = table.insertRow();    let target_column = row.insertCell(0);    target_column.innerHTML = zoom.target_column;    let source_column = row.insertCell(1);    source_column.innerHTML = zoom.filter_column;    let filter_attribute = row.insertCell(2);    filter_attribute.innerHTML = zoom.filter_attribute;    let abstraction_function = row.insertCell(3);    abstraction_function.innerHTML = zoom.abstraction_function;    let delete_cell = row.insertCell(4);    const deleteBtn = document.createElement("button");    deleteBtn.textContent = "❌";    delete_cell.appendChild(deleteBtn);    deleteBtn.onclick = async () => {      SPECIFIC_ZOOMS.specific_zooms.splice(index, 1);      renderSpecificZooms();      await draw();    };  });}